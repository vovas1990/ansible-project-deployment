---
- name: создание сети
  community.docker.docker_network:
   name: app_network
   driver: overlay
- name: выкладка сервисов
  include: "roles/deploy/services/{{ item }}/service.yml"
  vars:
   - name: "{{ item }}"
  loop: "{{ services }}"
